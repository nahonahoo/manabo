# まなぼ／まなぼみに — プロジェクト引き継ぎメモ

## これは何
「まなぼ」「まなぼみに」は、教えると育つ教育ペットアプリ。中学生の子どもが「まなぼ」を、幼稚園〜小1のいとこが「まなぼみに（カスタム名：みにぼ）」を、それぞれ自分のペットとして育てている。GitHub Pagesでホスティングされた個人プロジェクト。

## ホスティング・技術構成
- まなぼ： `nahonahoo.github.io/manabo/`
- まなぼみに： `nahonahoo.github.io/manabo/manabo-mini/`
- ショップ： `nahonahoo.github.io/manabo/manabo-shop/`
- 交流履歴： `nahonahoo.github.io/manabo/kouryu/`
- Firebase Firestore（projectId: `manabo-nhnh`）。まなぼ = doc `shared`、まなぼみに = doc `mini-shared`
- AI：Anthropic Claude API（`claude-haiku-4-5-20251001`）
- デプロイ運用：ファイルをダウンロード → ローカル（`C:/Users/bluej/Documents/benkyou-project/manabo/`）で動作確認 → GitHubリポジトリ（nahonahoo/manabo）へ手動アップロード

## ファイル構成（app.js分割済み・2026年8月時点）
まなぼ側は以下の10ファイル＋index.html＋style.cssの構成（**まなぼみに側も2026年8月にtrain-journey.jsを反映済み。同じ10ファイル構成**）：

```
manabo/
├─ index.html
├─ style.css
├─ js/
│  ├─ data.js            アイテムDB・レアリティ情報
│  ├─ state.js            S状態オブジェクト・Firebase保存/読込
│  ├─ core-ui.js          起動処理・ヘッダー・タブ・ドロワー・トースト
│  ├─ pet-animation.js    表情・瞬き・独り言ループ（まなぼみに側は花火演出も）
│  ├─ chat.js             Claude API呼び出し・チャット・おしえる
│  ├─ diary-knowledge.js  日記生成・記憶タブ・出品
│  ├─ customize.js        親モード・パーソナライズ・見た目カスタマイズ
│  ├─ social.js           お手紙・招待・おみやげ知識・3人会話シーンログ
│  ├─ train-journey.js    でんしゃのたび（路線図・旅行コース・メダル）
│  └─ shop-craft.js       発明・ショップ出品・コレクションボックス
└─ manabo-mini/（同じ10ファイル構成のまなぼみに版。train-journey.jsは口調・配色のみみに向けに調整、コース内容・座標データはまなぼ側と共通）
```

**保存アーキテクチャの重要な注意**：`coins`・`shopItems`・`inventory`・`craftCount`・`craftDate`・`saleHistory`・`collection`・`collectionSeenCount`・`letters`の8項目は、`saveState()`（チャットやXPなど通常時に呼ばれる一般保存）には**含めない**。これらは「まなぼみに」やショップなど外部からも書き換えられる可能性があるフィールドで、含めてしまうと関係ない操作のたびに古いメモリの値で上書きしてしまう事故が起きる（実際に何度か起きた）。これらを変更する操作（発明・出品・取り下げ・パートナーから買う・お手紙を読む等）は、必ず`saveShared({変更したフィールドだけ})`で狙って保存すること。新しい「外部から変わりうるフィールド」を追加するときは、このパターンに従うこと。`trainProgress`は逆に完全にローカル専用（外部から書き換えられない）なので、通常の`saveState()`に含めてよい。

元は単一の`app.js`（2500〜2600行）だったものを機能ごとに分割した。ESモジュールは不使用（`index.html`の`onclick`属性が大量にあるため、プレーンな`<script>`タグ方式を採用）。分割時は元コードを完全に再構成できることを検証済みで、挙動は変えていない。

**キャッシュ対策の注意**：各`<script src="js/xxx.js?v=N">`のNは、ファイルを更新するたび必ず変えること（連番でもランダムでもよい）。変えないとブラウザが古いファイルをキャッシュしたままになり、原因不明の不具合につながる（実際に`?v=1`のまま3回更新して発生した）。

**ファイル取り違え注意**：まなぼ側とまなぼみに側で同名ファイル（`social.js`など）が存在するため、アップロード時に取り違えやすい。過去に一度、まなぼ側ルートの`js/social.js`にまなぼみに側のものが誤って上書きされた事例あり。

## これまでの主な作業
1. デバッグ残骸の削除：`callGemini`内の生エラーをそのまま`alert()`表示していたのを削除し`console.warn`に変更（両サイド）。まなぼみに側`teachManabo`内にあった「デバッグ用」とコメントされた`showToast`も削除。
2. お手紙のMY_ID/PARTNER_ID整合性確認：まなぼ側（`MY_ID='shared'`）・まなぼみに側（`MY_ID='mini-shared'`）で対称になっていることを確認済み。
3. `app.js`分割：上記の9ファイル構成に分割。
4. 3人会話（招待セッション）の精度改善：
   - 招待中の会話に発言者付き共有シーンログ（`sceneLog` / `pushScene` / `sceneTranscript` / `resetScene`、`social.js`内）を導入。ホスト・ゲスト双方のプロンプトが直近の会話を参照できるようにした。
   - おみやげ知識生成（`generateOmiyage`）もシーンログ基準に変更し、発言者の取り違えを解消。
   - 「せんぱい」呼称の混同を修正：まなぼ（中学生・年上）だけがまなぼみに（幼稚園〜小1・年下）から「せんぱい」と呼ばれる。まなぼが自分を「まなぼせんぱい」と自称してしまうバグがあったため、全プロンプトに呼び方の注意書きを追加。
5. ファイル取り違えトラブルの解消（上記「注意」参照）。
6. Firestoreデータ混線の修正：まなぼ側で「最新データに更新する」を押したらまなぼみにの外見（オレンジ・星飾り）になっていた事象が発生。カスタマイズ画面の「デフォルトに戻す」ボタンで解消。まなぼみに側も名前が「まなぼ」になっていたのをユーザーが「みにぼ」に手動で直して解消。原因は未特定（ファイル取り違えとは別に、過去のテスト中にFirestoreへ互いのデータが書き込まれた可能性）。
7. **お店・コレクション周りの大規模改修（2026年8月）**：
   - まなぼ⇔まなぼみにが直接お互いのショップ商品を買えるように（`buyFromPartner`、`fsReadPartner`/`fsWritePartner`を流用）
   - パートナーから買ったアイテムは再出品不可の「コレクションボックス」（🎁ボタン）に入る仕様に変更。未読件数バッジは開くとリセットされる
   - コイン・お手紙が「関係ない操作のたびに消える」バグを繰り返し発見・修正し、最終的に上記「保存アーキテクチャ」（`saveState`/`saveShared`分離）で根本解決
   - ショップのNPC購入確率を引き上げ＋ショップを開いていない間の巡回を早送りするよう改善（売れやすくしてほしいという要望）
8. **「でんしゃのたび」機能を新規実装（2026年8月・まず まなぼ側のみ）**：東海エリアの実在10路線・40駅をモデルにした旅行ゲーム。コースを買うと、駅・路線制覇が進み、路線メダル（丸バッジ）＋実在の車両名メダル（のぞみ・ひのとり・しなの・ミュースカイ等、オーバル型ネームプレートデザイン）を獲得、コレクションボックスに貯まる。購入時は電車が横切る約10秒の出発アニメーション演出あり。路線図はデフォルメの地形背景＋SVGで自作し、OpenStreetMapの実座標（緯度経度）を投影して駅の位置関係を算出している（Jorudan等の商用路線図PDFは著作権があるため直接埋め込まず、位置関係の参考にのみ使用）。まなぼ・まなぼみにで進捗は完全に別々。
9. **「でんしゃのたび」の改修＆まなぼみにへの反映（2026年8月）**：
   - 路線図をピンチズーム／1本指ドラッグパン／ダブルタップズーム対応に（`user-scalable=no`のためネイティブピンチが効かず、Pointer Eventsで自前実装）。
   - コースごとに複数の車両メダルを獲得できるよう`vehicle`（単数）→`vehicles`（配列）に変更。値段が高いコースほど乗り換える路線・車両が増えるよう10コースを再設計し、価格帯を¥3,000〜¥26,000に圧縮（東海地方のみの現状に合わせる）。「全部まとめてもらえる総仕上げコース」（旧・東海オールスター大旅行）は安いコースの価値を消してしまうため廃止し、代わりに「ミュースカイでひとっとび」を新設。
   - 上記すべてを`manabo-mini/`側にも反映（`js/train-journey.js`をみにぼの口調・配色向けに書き換えつつ、路線・駅・コース内容はまなぼ側と共通のデータを使用。`js/state.js`の`trainProgress`に`riddenVehicles`追加、`js/shop-craft.js`に`vehicle-medal`表示分岐追加、`index.html`に`train-travel-overlay`・ピンチズーム用スタイル追加、`style.css`に出発アニメーションのkeyframes追加）。
   - **この改修中に本番Firebaseへの誤書き込み事故が発生**：ローカルのpython http.serverで動かしても本番Firestore（テスト用の別環境は存在しない）に直接読み書きされることに気づかず、動作確認のつもりで購入処理を直接実行してしまい、まなぼの所持金が誤って99万円に、コレクションボックスに偽メダル10件が追加される事故が起きた。ユーザーの記憶（¥5,655）を頼りに復旧。詳細は[[feedback_manabo_no_test_env_live_writes]]の記憶を参照。以降、状態を変更する関数（`buyTrainCourse`等）はローカルテストで絶対に実行せず、確認は読み取り専用（`S`の中身を読む・レンダリングだけ行う・保存直後に必ずメモリ上の値を元の値へ戻す）に限定する運用に変更。

10. **「？クイズ」機能を新規実装（2026年8月・まなぼ／まなぼみに両方）**：「教える動機が湧かず、レベルも発明もお金もなかなか育たない」という報酬体験の断絶を解消するための機能。既存の独り言ループ（`doMono`。中3受験頻出の問いを投げるパターンCなど）や「おしえる」フローの`question`フィールド（教えた内容への関連質問）は元々あったが、どちらも表示するだけで答えさせる仕組みも報酬もなかった。この土台を流用し、独り言ループと同じペース（3〜10分に1回・1日20回まで）で「？」マークがペットの右上に出現するようにし、タップすると一問一答クイズが出題され、正解すると¥50＋XP1がもらえる仕組みを追加した。
   - 出題範囲はまなぼ＝「小学校〜中学3年生1学期（夏休み前）まで」、まなぼみに＝「小学2年生1学期（夏休み前）まで」に、AIへのシステムプロンプトで明示的に固定（未習範囲を出題しないため）。
   - 出題・採点はAIの1回の呼び出しで完結：`{"question":..., "answer":..., "acceptableAnswers":[...]}`をJSONで生成させ、正誤判定はAIを再度呼ばず、`acceptableAnswers`とのローカルな表記ゆれ許容マッチ（前後空白除去・句読点除去・大文字小文字統一・部分一致）で判定する。
   - `pendingQuiz`（未回答のクイズ）は答えるかタイムアウトなしで残り続ける設計。次の出現タイミングが来ても、答えていないクイズがある間は新しいクイズを生成しない（無駄なAPI呼び出し防止）。
   - 1日の獲得上限は設けていない（ユーザーの意向。「？」の出現頻度自体が独り言ループと同じペースに抑えられているため、そこが自然な歯止めになる）。
   - `js/state.js`に`quizCount`/`quizDate`を追加（`monoCount`/`monoDate`と全く同じ日次リセットパターン）。`coins`の更新は`saveShared({coins:...})`で行い、通常の`saveState()`には含めない（保存アーキテクチャのルール通り）。
   - 「パス」ボタンを追加（✕で閉じるのとは役割が違う：✕は保留＝？マークが残って後でまた答えられる、パスはこの問題を破棄＝？マークが消えて次の出現タイミングで新しい問題に変わる。ペナルティなし・保存処理も呼ばない）。
   - 実際に使ってみたところ「？マークがあんまり出ない」というフィードバックがあり、①出現ペースを独り言ループと同じ3〜10分→1〜3分に短縮、②AIの応答が純粋なJSONで返らず静かに失敗していた可能性に対応して`sendChat`の`reply`抽出と同じ考え方で`question`/`answer`を正規表現フォールバック抽出するように修正（失敗時は`console.warn`でraw応答を出力するようにもした）。1日20回の上限は変更していない（ペースだけ約3倍速くなったので、上限に先に達する日が増える可能性はある。気になるようなら上限も上げる）。

11. **日記→アイテムボックスまわりのバグ修正・整理（2026年8月）**：
    - 「発表」「詩」の日記タイプを両サイドから削除（`js/diary-knowledge.js`の`prompts`/`lines`オブジェクトと、`index.html`のボタンから除去。ユーザーが「授業で発表はあまり意味を感じない」「詩もいらない」と判断）。残るのは「日記」「小説」（＋招待中の合同日記/小説、まなぼは「ひらめき日記」）。
    - ボタン文言を「この日記をショップに出品する」→「この日記に値段をつける」に変更。`sellDiaryWork()`はAIに評価させて`S.inventory`（アイテムボックス）に追加するだけで、実際にショップへ出品する（`S.shopItems`に移す＝`listItemToShop()`）のは別の独立した操作なので、文言が実態と合っていなかった。
    - **重大なバグを発見・修正**：`sellDiaryWork()`は成功時に`btn.disabled`を`false`に戻す処理がなく（失敗時のリトライ用リセットだけ存在していた）、一度値段づけに成功すると、次に新しい作品を書いてボタンが再表示されても永久に押せなくなっていた。`generateDiary()`側でボタンを再表示する箇所に`btn.disabled = false`を追加して解消（まなぼ・まなぼみに両方に同じ修正を適用）。
    - **ついでに発見した無関係な既存バグ**：まなぼみに側の`index.html`のペットSVGで、影の`<ellipse>`に`id="shadow-e"`が付いておらず、`bounce()`（ジャンプ演出）のたびに影のアニメーション処理が`null`エラーで毎回静かに失敗していた。IDを追加して修正。

## 既知の課題
- 東海以外の地域路線図は未着手。拡張時は[[project_train_journey_regional_maps]]の記憶にある地域別PDF（北海道〜九州）を必ず参照すること（座標は勘やAIの一般知識だけで決めない）。
- まなぼみに側の`でんしゃのたび`・`？クイズ`はコード上は完成しているが、実機（実際のiPad/スマホ）での実動作確認はまだ行っていない（本番Firebase書き込み事故を受けて、ローカルでの購入・報酬確定テストを避け、`saveShared`/`saveState`をモック化した状態でのUI・ロジック確認のみに留めたため）。次回まなぼ・みにぼが実際に遊んだときに問題がないか確認するとよい。特に？クイズはAPIキーがない状態ではテストできなかったため、実際のAI応答が期待通りのJSON形式で返ってくるか（`parseJSON`が失敗しないか）は未確認。

## 将来の予定（未着手）
- **サキタン・ユ／カクズン（いとこの両親）の買い物ボタン機能**：それぞれ専用のホーム画面アプリ＋所持金を持ち、「買い物に行く」ボタンを押すとシンプルにショップの商品を選んで買ってアイテムを溜め込める、という構想。まだ設計段階（キャラの口調・性格・好みのデータは既にどこかに送ってある、とユーザー談）。
- **カードガチャ機能**：貯まった円でガチャを引いてカードを収集。アイテムボックスにカードも入れて捨てられるようにする。
- **「おじぼショップ」構想（保留中・ユーザーが「まだ作らなくていい」と保留）**：まなぼ型のおじさんキャラ「おじぼ」が経営する独立した新ページ（例：`ojibo-shop/`）。
  - 表メニュー：SVGアイテムを1個300円で販売
  - 裏メニュー：「世界の英知カード」を1回5000円のガチャ形式で販売（まなぼ王国では禁制品という設定）
  - カードは50枚予定、イラスト画像をそのまま使う方針（アインシュタイン・ダ・ヴィンチ・織田信長・クレオパトラ・ニコラ・テスラ・ピラミッド・マチュピチュ・ストーンヘンジの8枚が既存サンプル）
  - カード構成要素：連番／名前／カテゴリー（歴史を動かした人物・驚異の建造物など）／二つ名／説明文／ATK・DEF
  - 未確定：排出確率、カテゴリーの追加予定有無、表メニューを同時実装するか

## キャラ設定サマリー
（詳細は元の仕様書 `manabo-spec.md` を参照。要点のみ）

**まなぼ（中学生向け）**：性別なし・妖精みたいな存在。語尾「〜ぼ」「だぼ」「ぎゃぼー」など。「せんぱい」と呼ばれるのはまなぼだけ（まなぼみにから）。自分から名乗らない。

**まなぼみに（幼稚園〜小1向け、カスタム名「みにぼ」）**：生意気で鋭くて好奇心旺盛。語尾「〜だよ！」「えへへ」など。まなぼを「まなぼせんぱい」と呼ぶ。

## 次のセッションで最初に確認すること
- claude.aiで続ける場合：Claudeのメモリ機能（`/areas/manabo.md`）にも同内容が保存されているので、そちらも自動的に参照される。
- Claude Codeで続ける場合：このファイルをプロジェクトルートに `CLAUDE.md` という名前で置いておくと、セッション開始時に自動的に読み込まれる。
